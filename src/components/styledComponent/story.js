import { h } from 'preact';
import { storiesOf } from '@storybook/preact';
import { StyledComponent } from '.';

storiesOf('Test Component', module).add('y u no work', () => (
	<StyledComponent />
));
